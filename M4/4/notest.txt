1. Логическое проектирование (моделирование БД)
Цель: создать абстрактную модель, отражающую реальную предметную область.
Описывает объекты, события и факты реального мира.
Создаёт логическую модель данных, независимую от конкретной СУБД.
Ориентировано на понятность, полноту и непротиворечивость.
Пример результата: диаграмма сущностей и связей (ERD).


2. Физическое проектирование (построение БД)
Цель: воплотить логическую модель в конкретной СУБД и оптимизировать хранение данных.
Учитывает возможности и ограничения конкретной СУБД.
Включает настройку физических структур:
Таблицы
Индексы
Триггеры
Представления (views)
Оптимизирует доступ к данным и их обработку.
Может требовать адаптации логической модели для лучшей производительности.


Логическое проектирование реляционной базы данных
Реляционная БД — это набор связанных таблиц (отношений).
Основные задачи логического проектирования:
1. Определение набора таблиц
Выделить все сущности предметной области.
Каждая сущность → отдельная таблица.
Таблицы должны покрывать все объекты и процессы, важные для системы.
2. Определение атрибутов
Для каждой таблицы задаются её атрибуты (колонки).
Указывается тип данных для каждого атрибута (число, текст, дата и т.д.).
Обязательное определение первичного ключа (PK) — уникальный идентификатор строки.
3. Определение связей между таблицами
Связи формируются с помощью внешних ключей (FK).
FK указывает на PK в другой таблице.
Обеспечивает логическую целостность данных.
💡 Важно:
Хорошее логическое проектирование облегчает разработку, поддержку и использование базы.
Это этап моделирования, который независим от конкретной СУБД.

Аномалии в реляционных БД
Цель хорошей схемы БД:
Корректно и однозначно отражать все ситуации предметной области.
Исключать возможность противоречивых данных (логических, не фактических).
Что такое аномалии организации данных?
Аномалия — это ситуация, при которой:
Данные нельзя корректно внести в базу.
Можно внести данные, нарушающие правила предметной области.
Аномалии возможны в любой схеме, но хорошая схема — та, где их вероятность минимальна.
Причины появления аномалий:
Изменение правил предметной области
Реальность развивается, и модель может устареть.
Недостаточная информированность разработчика
Неполное понимание предметной области приводит к недочётам в проектировании.
Небрежное проектирование
Ошибки, допущенные при логическом проектировании.
Ограничения на моделирование
Некоторые требования сложно (или невозможно) полностью выразить в реляционной схеме.
💡 Вывод: Аномалии — это не ошибка данных, а ошибка структуры. Для борьбы с ними используют нормализацию и продуманные проектные решения.

📌 Аномалии типов данных и доменов
Что это?
Аномалии возникают, когда для поля в таблице неправильно определены:
Тип данных
Домен (набор допустимых значений)
❗ Типичные примеры:
Неправильный тип данных
Поле «номер документа» задано как число, но фактически содержит значения типа: А10-03, Б12-07.
Итог: такие данные не получится внести, потеря информации.
Неполный домен значений
Поле «пол сотрудника» задано числовым типом, домен:
0 = мужской, 1 = женский

Проблемы:
Нет запретов на ввод других чисел (например, 2 или 10).
Ограничение "только 2 пола" неверно — например, у пассажиров возможен вариант «ребенок».
🎯 Вывод:
Указывайте корректный тип данных (например, VARCHAR вместо INT для кодов с буквами).
Настраивайте домен явно (через CHECK, ENUM или справочники).
Продумывайте все возможные значения на этапе проектирования, чтобы избежать ошибок и нарушений целостности данных.


Вот шпаргалка в простом формате по теме аномалий целостности данных:

---

Аномалии целостности данных
Что это: возникают при нарушении базовых принципов целостности данных.
Пример:
* В таблице документов выбран первичный ключ: НомерДокумента.
* Ограничения: уникальность и запрет на пустое значение (NULL).
* Проблема: нумерация документов каждый год начинается с 1.
* Следствие: один и тот же номер документа может повторяться в разные годы.
* Итог: по правилам предметной области документ определяется комбинацией двух полей — НомерДокумента и ДатаДокумента. Но схема БД это не учитывает, и данные внести невозможно.

Вывод:
* Нужно правильно выбирать первичные ключи и учитывать все правила предметной области.
* Нарушение целостности приводит к аномалиям и невозможности корректного внесения данных.


Вот шпаргалка в чистом и удобном формате по аномалиям избыточного дублирования:

---

Аномалии избыточного дублирования
Что это: возникают, когда одни и те же данные повторяются в нескольких записях, что приводит к проблемам при добавлении, изменении или удалении данных.
Пример: таблица «Пассажиры»

* Каждый рейс определяется Номером Рейса, имеет направление и выполняется компанией.
* Для каждого пассажира приходится повторно записывать данные рейса (НомерРейса, Направление, Компания).
* Чем больше пассажиров, тем больше повторяющихся данных.

Проблемы:

1. **Аномалия добавления**

   * При добавлении нового пассажира на рейс может возникнуть противоречие: по одним записям направление — Москва, по другим — Минск.
   * Результат: неопределенность данных о рейсе.

2. **Аномалия изменения**

   * Если изменяется информация о рейсе (например, компания), нужно менять все записи пассажиров этого рейса.
   * Иначе данные становятся противоречивыми (разные компании для одного рейса).

3. **Аномалия удаления**

   * Если удалить пассажира, который был единственным на рейсе, теряются данные о рейсе.
   * Проблема: потеря информации о рейсе при удалении связанных записей.

4. **Проблема с пустыми связями**

   * Если на рейс пока нет пассажиров, данные о рейсе невозможно внести, так как таблица «Пассажиры» содержит только записи о пассажирах.

Вывод:

* Избыточное дублирование создаёт **неопределенность, ошибки и потерю данных**.
* Решение: нормализовать базу, выделив отдельную таблицу для рейсов, чтобы данные о рейсе хранились только один раз.

Нормализация
Что это: процесс приведения структуры базы данных к такому состоянию, при котором данные хранятся эффективно и без избыточного дублирования.
Основная идея: информация о каком-либо объекте (факте, событии) хранится только в одном месте и в единственном экземпляре. Если информация нужна в другом месте, вместо дублирования используется ссылка на запись (в реляционной модели — через внешний ключ).
В реляционной модели: структурная единица хранения информации — таблица. Информация об объекте = запись в таблице. Ссылки на эту информацию = внешние ключи в других таблицах.
Цель нормализации: данные об объекте хранятся только в одной записи и одной таблице. В других таблицах, где нужны атрибуты этой записи, ставится внешний ключ, ссылающийся на нужную запись.
Нормальные формы
Нормальная форма — структура базы данных, в которой соблюдаются определённые правила хранения данных.
Степень нормализации может быть разной — от низкой до высокой. Каждая последующая нормальная форма включает правила всех предыдущих.
Основные нормальные формы:

Первая нормальная форма (1НФ)
Вторая нормальная форма (2НФ)
Третья нормальная форма (3НФ)
Нормальная форма Бойса-Кодда (BCNF)
Четвёртая нормальная форма (4НФ)
Пятая нормальная форма (5НФ)
Практическая заметка: в большинстве проектов достаточно соблюдать первые три нормальные формы.
Высшие нормальные формы (BCNF и выше) редко применяются на практике.


Первая нормальная форма (1НФ)
Суть: все атрибуты таблицы содержат только атомарные (неделимые) значения; каждая запись уникальна.
Условия таблицы в 1НФ:
Порядок строк не несёт информации.
Порядок столбцов не несёт информации.
Нет повторяющихся строк.
Каждое пересечение строки и столбца содержит только одно значение.
Все столбцы «обычные», без скрытых идентификаторов или временных меток.
Вариант 2: отсутствуют группы повторяющихся значений; повторяющиеся данные выделяются в отдельные таблицы с первичными ключами и связью «один-ко-многим».
Пример: таблица «Студенты» с повторяющимися данными о курсах → выделена отдельная таблица «Пройденные курсы» с составным ключом (КодСтудента + КодКурса).

Вторая нормальная форма (2НФ)
Суть: таблица находится во 2НФ, если все неключевые атрибуты зависят от всего составного первичного ключа, а не от части ключа.
Что проверяем: нет частичных зависимостей.
Исправление нарушения: выделяем зависимые атрибуты в отдельную таблицу; часть первичного ключа становится ключом новой таблицы; связь «один-ко-многим» от новой таблицы к старой.
Пример: в таблице «Пройденные курсы» поле «ОписаниеКурса» зависит только от «КодКурса» (часть составного ключа). → создаём таблицу «Курсы» с первичным ключом КодКурса и связываем через внешний ключ.

Третья нормальная форма (3НФ)
Суть: таблица находится в 3НФ, если неключевые атрибуты не зависят друг от друга, а только от первичного ключа.
Исправление нарушения: выделяем зависимые неключевые атрибуты в отдельную таблицу; используем зависимый атрибут в качестве ключа новой таблицы; связь «один-ко-многим» к исходной таблице.
Пример: в таблице «Студенты» поля «КодМенеджера» и «Менеджер» связаны между собой → создаём таблицу «Менеджеры» и связываем через «КодМенеджера».
Вывод:
1НФ → атомарные значения, нет повторяющихся групп.
2НФ → нет частичных зависимостей от составного ключа.
3НФ → нет транзитивных зависимостей между неключевыми атрибутами.
Результат нормализации: база данных без повторов, неопределённостей и легко поддерживаемая.
